bug square(x) {
    return x * x;
}

bug cube(x) {
    return x * x * x;
}

bug add(x, y) {
    return x + y;
}

a = 2;
b = add(1, square(cube(a)));

if (a == 2) {
    print(1);
} else {
    print(2);
}
