bug square(x) {
    return x * x;
}

bug cube(x) {
    return x * x * x;
}

bug add(x, y) {
    return x + y;
}

a = 1;
b = add(1, square(cube(a)));
c = 1;

while (c <= 100000000) {
    print(c);

    if (c == 1) {
        c = 2;
    } else {
        c = c * 2;
    }
}
